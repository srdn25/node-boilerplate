createExample:
  $ref: './schemas.yaml#example'

patchExample:
  type: object
  properties:
    name:
      type: string
      example: example name
    status:
      type: string
      example: disabled
